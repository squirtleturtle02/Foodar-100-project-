function testZip(zip) {
if(zip > 20000) {
    return true;
} else {
    return false;
}
}
var zip = 5000
testZip(zip);
//check if zipcode is greater than 20000
//if so return true
//if not return false